# Calculator.py

```
  All possible python math operations are present in the calculator.
  The calculator has division by zero protection.
  The program writes the history of calculations to the list (To display the list, enter "history" when selecting an operation). 
  The program will exit if you enter "exit" when selecting an operation.
```
